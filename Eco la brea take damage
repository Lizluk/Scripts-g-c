local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local UserInputService = game:GetService("UserInputService")

-- Bộ lưu trữ dữ liệu
local RemoteLogs = {}      
local BlockedPaths = {}   -- Thay vì chặn Instance, chuyển sang chặn theo Đường dẫn chính xác (Full Name Path)
local SelectedRemoteId = nil 

-- Bộ lọc trạng thái
local Filters = {
    RemoteEvent = true,
    RemoteFunction = true,
    BindableEvent = true,   
    BindableFunction = true
}

-- Dọn dẹp GUI cũ
local oldGui = LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("HydroSpyV9Pro")
if oldGui then oldGui:Destroy() end

-- =========================================================================
-- BỘ CHUYỂN ĐỔI THAM SỐ
-- =========================================================================
local function getArgDetails(v)
    local t = typeof(v)
    if t == "string" then return "String", string.format('"%s"', v), string.format('"%s"', v)
    elseif t == "number" then return "Number", tostring(v), tostring(v)
    elseif t == "boolean" then return "Boolean", tostring(v):upper(), tostring(v)
    elseif t == "Instance" then return "Instance", (v and v:GetFullName() or "nil"), string.format('game.%s', (v and v:GetFullName() or "nil"))
    elseif t == "Vector3" then 
        return "Vector3", string.format("(%.2f, %.2f, %.2f)", v.X, v.Y, v.Z), string.format("Vector3.new(%.2f, %.2f, %.2f)", v.X, v.Y, v.Z)
    elseif t == "table" then
        local success, json = pcall(function() return HttpService:JSONEncode(v) end)
        return "Table", success and json or "{...}", success and json or "{}"
    else
        return t, tostring(v), tostring(v)
    end
end

-- =========================================================================
-- KHỞI TẠO GIAO DIỆN CHUẨN HYDROSPY
-- =========================================================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "HydroSpyV9Pro"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 680, 0, 380)
MainFrame.Position = UDim2.new(0.5, -340, 0.4, -190)
MainFrame.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
MainFrame.BorderSizePixel = 1
MainFrame.BorderColor3 = Color3.fromRGB(40, 40, 45)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local TopBar = Instance.new("Frame")
TopBar.Size = UDim2.new(1, 0, 0, 35)
TopBar.BackgroundColor3 = Color3.fromRGB(16, 16, 20)
TopBar.BorderSizePixel = 0
TopBar.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(0, 220, 1, 0)
Title.Position = UDim2.new(0, 12, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "⚡ HydroSpy v9.0 Pro [Phím P để Ẩn]"
Title.TextColor3 = Color3.fromRGB(170, 180, 200)
Title.TextSize = 13
Title.Font = Enum.Font.Code
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = TopBar

-- ẨN / HIỆN GUI BẰNG PHÍM P
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.P then
        MainFrame.Visible = not MainFrame.Visible
    end
end)

local FilterContainer = Instance.new("Frame")
FilterContainer.Size = UDim2.new(0, 400, 1, 0)
FilterContainer.Position = UDim2.new(1, -410, 0, 0)
FilterContainer.BackgroundTransparency = 1
FilterContainer.Parent = TopBar

local FilterLayout = Instance.new("UIListLayout")
FilterLayout.FillDirection = Enum.FillDirection.Horizontal
FilterLayout.HorizontalAlignment = Enum.HorizontalAlignment.Right
FilterLayout.VerticalAlignment = Enum.VerticalAlignment.Center
FilterLayout.Padding = UDim.new(0, 6)
FilterLayout.Parent = FilterContainer

local LeftPanel = Instance.new("ScrollingFrame")
LeftPanel.Size = UDim2.new(0.4, -8, 1, -45)
LeftPanel.Position = UDim2.new(0, 6, 0, 40)
LeftPanel.BackgroundColor3 = Color3.fromRGB(14, 14, 16)
LeftPanel.BorderColor3 = Color3.fromRGB(30, 30, 35)
LeftPanel.CanvasSize = UDim2.new(0, 0, 0, 0)
LeftPanel.ScrollBarThickness = 4
LeftPanel.Parent = MainFrame

local LeftLayout = Instance.new("UIListLayout")
LeftLayout.Padding = UDim.new(0, 2)
LeftLayout.Parent = LeftPanel

local RightPanel = Instance.new("Frame")
RightPanel.Size = UDim2.new(0.6, -4, 1, -45)
RightPanel.Position = UDim2.new(0.4, 2, 0, 40)
RightPanel.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
RightPanel.BorderSizePixel = 0
RightPanel.Visible = false
RightPanel.Parent = MainFrame

local DetailTitle = Instance.new("TextLabel")
DetailTitle.Size = UDim2.new(1, -100, 0, 30)
DetailTitle.Position = UDim2.new(0, 8, 0, 0)
DetailTitle.BackgroundTransparency = 1
DetailTitle.Text = "Remote"
DetailTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
DetailTitle.TextSize = 13
DetailTitle.Font = Enum.Font.SourceSansBold
DetailTitle.TextXAlignment = Enum.TextXAlignment.Left
DetailTitle.Parent = RightPanel

local BlockButton = Instance.new("TextButton")
BlockButton.Size = UDim2.new(0, 80, 0, 24)
BlockButton.Position = UDim2.new(1, -86, 0, 3)
BlockButton.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
BlockButton.BorderColor3 = Color3.fromRGB(55, 55, 60)
BlockButton.Text = "⬜ Chặn"
BlockButton.TextColor3 = Color3.fromRGB(200, 200, 200)
BlockButton.TextSize = 12
BlockButton.Font = Enum.Font.SourceSans
BlockButton.Parent = RightPanel

local LogsFrame = Instance.new("ScrollingFrame")
LogsFrame.Size = UDim2.new(1, -12, 1, -36)
LogsFrame.Position = UDim2.new(0, 6, 0, 32)
LogsFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 16)
LogsFrame.BorderColor3 = Color3.fromRGB(28, 28, 32)
LogsFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
LogsFrame.ScrollBarThickness = 4
LogsFrame.Parent = RightPanel

local RightLayout = Instance.new("UIListLayout")
RightLayout.Padding = UDim.new(0, 6)
RightLayout.Parent = LogsFrame

local ContextMenu = Instance.new("Frame")
ContextMenu.Size = UDim2.new(0, 160, 0, 32)
ContextMenu.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
ContextMenu.BorderColor3 = Color3.fromRGB(60, 60, 65)
ContextMenu.ZIndex = 10
ContextMenu.Visible = false
ContextMenu.Parent = ScreenGui

local GenScriptBtn = Instance.new("TextButton")
GenScriptBtn.Size = UDim2.new(1, 0, 1, 0)
GenScriptBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
GenScriptBtn.BorderSizePixel = 0
GenScriptBtn.Text = "📄 Generate Script (Copy)"
GenScriptBtn.TextColor3 = Color3.fromRGB(230, 230, 230)
GenScriptBtn.TextSize = 12
GenScriptBtn.Font = Enum.Font.SourceSans
GenScriptBtn.ZIndex = 11
GenScriptBtn.Parent = ContextMenu

LeftLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    LeftPanel.CanvasSize = UDim2.new(0, 0, 0, LeftLayout.AbsoluteContentSize.Y)
end)
RightLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    LogsFrame.CanvasSize = UDim2.new(0, 0, 0, RightLayout.AbsoluteContentSize.Y)
end)

UserInputService.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.MouseButton2 then
        task.wait(0.1)
        ContextMenu.Visible = false
    end
end)

-- =========================================================================
-- HÀM LỌC HIỂN THỊ
-- =========================================================================
local function refreshLeftPanel()
    for _, btn in pairs(LeftPanel:GetChildren()) do
        if btn:IsA("TextButton") then
            local targetId = tonumber(btn.Name)
            local data = RemoteLogs[targetId]
            if data then
                btn.Visible = (Filters[data.ClassName] == true)
                if not btn.Visible and SelectedRemoteId == targetId then
                    RightPanel.Visible = false
                end
            end
        end
    end
end

local function createCheckbox(filterKey, icon)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 72, 0, 24)
    btn.BackgroundColor3 = Color3.fromRGB(30, 130, 70) 
    btn.Text = "✓ " .. icon
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.TextSize = 11
    btn.Font = Enum.Font.Code
    btn.Parent = FilterContainer
    
    btn.MouseButton1Click:Connect(function()
        Filters[filterKey] = not Filters[filterKey]
        if Filters[filterKey] then
            btn.BackgroundColor3 = Color3.fromRGB(30, 130, 70)
            btn.Text = "✓ " .. icon
        else
            btn.BackgroundColor3 = Color3.fromRGB(45, 45, 50) 
            btn.Text = "✗ " .. icon
        end
        refreshLeftPanel()
    end)
end

createCheckbox("RemoteEvent", "⚡ R.Evt")
createCheckbox("RemoteFunction", "🔮 R.Fnc")
createCheckbox("BindableEvent", "⚡ B.Evt")
createCheckbox("BindableFunction", "🔮 B.Fnc")

-- =========================================================================
-- XEM CHI TIẾT LOG BÊN PHẢI
-- =========================================================================
local function viewRemoteLogs(remoteId)
    SelectedRemoteId = remoteId
    local remoteData = RemoteLogs[remoteId]
    if not remoteData then return end
    
    local prefix = (remoteData.ClassName:find("Function") and "🔮 " or "⚡ ")
    DetailTitle.Text = prefix .. remoteData.Name
    
    for _, child in pairs(LogsFrame:GetChildren()) do
        if child:IsA("Frame") then child:Destroy() end
    end
    
    -- Kiểm tra trạng thái chặn dựa theo Path chuỗi duy nhất
    if BlockedPaths[remoteData.Path] then
        BlockButton.BackgroundColor3 = Color3.fromRGB(150, 45, 45)
        BlockButton.Text = "🟥 Đang Chặn"
    else
        BlockButton.BackgroundColor3 = Color3.fromRGB(35, 35, 40)
        BlockButton.Text = "⬜ Chặn"
    end
    
    for _, logData in ipairs(remoteData.Logs) do
        local LogBox = Instance.new("Frame")
        LogBox.Size = UDim2.new(1, -6, 0, #logData.DisplayArgs * 20 + 12)
        LogBox.BackgroundColor3 = Color3.fromRGB(22, 22, 26)
        LogBox.BorderColor3 = Color3.fromRGB(40, 40, 45)
        LogBox.Parent = LogsFrame
        
        local BoxLayout = Instance.new("UIListLayout")
        BoxLayout.Padding = UDim.new(0, 2)
        BoxLayout.Parent = LogBox
        
        local padding = Instance.new("UIPadding")
        padding.PaddingTop = UDim.new(0, 4)
        padding.PaddingLeft = UDim.new(0, 8)
        padding.Parent = LogBox
        
        for _, argText in ipairs(logData.DisplayArgs) do
            local argLabel = Instance.new("TextLabel")
            argLabel.Size = UDim2.new(1, 0, 0, 18)
            argLabel.BackgroundTransparency = 1
            argLabel.Text = argText
            argLabel.TextColor3 = Color3.fromRGB(210, 150, 90)
            argLabel.TextSize = 12
            argLabel.Font = Enum.Font.Code
            argLabel.TextXAlignment = Enum.TextXAlignment.Left
            argLabel.Parent = LogBox
        end
        
        LogBox.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton2 then
                ContextMenu.Position = UDim2.new(0, input.Position.X, 0, input.Position.Y)
                ContextMenu.Visible = true
                
                GenScriptBtn.MouseButton1Click:Connect(function()
                    ContextMenu.Visible = false
                    local method = "FireServer"
                    if remoteData.ClassName == "RemoteFunction" then method = "InvokeServer"
                    elseif remoteData.ClassName == "BindableEvent" then method = "Fire"
                    elseif remoteData.ClassName == "BindableFunction" then method = "Invoke" end
                    
                    local codeString = string.format("-- Generated by HydroSpy V9 Pro\nlocal Target = game.%s\nTarget:%s(%s)", logData.Path, method, table.concat(logData.CodeArgs, ", "))
                    if setclipboard then
                        setclipboard(codeString)
                        print("📋 Đã copy đoạn mã vào Clipboard thành công!")
                    end
                end)
            end
        end)
    end
    RightPanel.Visible = true
end

BlockButton.MouseButton1Click:Connect(function()
    if not SelectedRemoteId or not RemoteLogs[SelectedRemoteId] then return end
    local rPath = RemoteLogs[SelectedRemoteId].Path
    if not rPath then return end
    
    BlockedPaths[rPath] = not BlockedPaths[rPath]
    viewRemoteLogs(SelectedRemoteId)
    
    local itemBtn = LeftPanel:FindFirstChild(tostring(SelectedRemoteId))
    if itemBtn then
        itemBtn.TextColor3 = BlockedPaths[rPath] and Color3.fromRGB(255, 90, 90) or Color3.fromRGB(220, 220, 230)
    end
end)

-- GHI NHẬN LOG
local function addNewLog(remoteInstance, rawArgs)
    if not remoteInstance or not remoteInstance.Parent then return end
    
    local rPath = remoteInstance:GetFullName()
    
    -- Nếu đường dẫn cụ thể này đang bị chặn, dừng việc ghi log ngay lập tức
    if BlockedPaths[rPath] == true then return end
    
    local name = remoteInstance.Name
    local className = remoteInstance.ClassName
    local targetId = nil
    
    for id, data in pairs(RemoteLogs) do
        if data.Path == rPath then targetId = id break end
    end
    
    if not targetId then
        targetId = #RemoteLogs + 1
        RemoteLogs[targetId] = { Name = name, ClassName = className, Path = rPath, Logs = {} }
    end
    
    local displayList = {}
    local codeList = {}
    for i, v in ipairs(rawArgs) do
        local argType, displayVal, codeVal = getArgDetails(v)
        table.insert(displayList, string.format("[%d] 📦 Type: %s | Value: %s", i, argType, displayVal))
        table.insert(codeList, codeVal)
    end
    if #rawArgs == 0 then table.insert(displayList, "   (No Arguments)") end
    
    table.insert(RemoteLogs[targetId].Logs, { DisplayArgs = displayList, CodeArgs = codeList, Path = rPath })
    
    local existingBtn = LeftPanel:FindFirstChild(tostring(targetId))
    local count = #RemoteLogs[targetId].Logs
    local prefix = className:find("Function") and "🔮" or "⚡"
    
    if existingBtn then
        existingBtn.Text = string.format("  %s (%d) %s", prefix, count, name)
        if SelectedRemoteId == targetId then viewRemoteLogs(targetId) end
    else
        local RemoteItem = Instance.new("TextButton")
        RemoteItem.Name = tostring(targetId)
        RemoteItem.Size = UDim2.new(1, -6, 0, 30)
        RemoteItem.BackgroundColor3 = Color3.fromRGB(26, 26, 30)
        RemoteItem.BorderSizePixel = 0
        RemoteItem.Text = string.format("  %s (1) %s", prefix, name)
        RemoteItem.TextColor3 = Color3.fromRGB(220, 220, 230)
        RemoteItem.TextSize = 13
        RemoteItem.Font = Enum.Font.SourceSans
        RemoteItem.TextXAlignment = Enum.TextXAlignment.Left
        RemoteItem.Parent = LeftPanel
        
        RemoteItem.MouseButton1Click:Connect(function()
            for _, child in pairs(LeftPanel:GetChildren()) do
                if child:IsA("TextButton") then child.BackgroundColor3 = Color3.fromRGB(26, 26, 30) end
            end
            RemoteItem.BackgroundColor3 = Color3.fromRGB(40, 40, 46)
            viewRemoteLogs(targetId)
        end)
        
        RemoteItem.Visible = (Filters[className] == true)
    end
end

-- =========================================================================
-- HOOK THEO PATH ĐỘC LẬP - KHÔNG BỊ LẪN LỘN CÁC LỆNH KHÁC
-- =========================================================================
local success, err = pcall(function()
    local rawMetatable = getrawmetatable(game)
    local setReadOnly = setreadonly or make_writeable
    
    setReadOnly(rawMetatable, false)
    local oldNamecall = rawMetatable.__namecall

    rawMetatable.__namecall = newcclosure(function(self, ...)
        local method = getnamecallmethod()
        local args = {...}
        
        if not checkcaller() and self and self.Parent then
            local successPath, rPath = pcall(function() return self:GetFullName() end)
            
            if successPath then
                -- 1. Xử lý RemoteEvent
                if method == "FireServer" and self:IsA("RemoteEvent") then
                    if BlockedPaths[rPath] == true then return end 
                    task.spawn(function() addNewLog(self, args) end)
                    
                -- 2. Xử lý RemoteFunction
                elseif method == "InvokeServer" and self:IsA("RemoteFunction") then
                    if BlockedPaths[rPath] == true then return nil end 
                    task.spawn(function() addNewLog(self, args) end)
                    
                -- 3. Xử lý BindableEvent
                elseif method == "Fire" and self:IsA("BindableEvent") then
                    task.spawn(function() addNewLog(self, args) end)
                    
                -- 4. Xử lý BindableFunction
                elseif method == "Invoke" and self:IsA("BindableFunction") then
                    task.spawn(function() addNewLog(self, args) end)
                end
            end
        end
        
        return oldNamecall(self, ...)
    end)

    setReadOnly(rawMetatable, true)
end)

if not success then warn("Executor không hỗ trợ can thiệp sâu Metatable:", err) end
