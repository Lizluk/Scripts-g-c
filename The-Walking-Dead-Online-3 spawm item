local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Xóa GUI cũ nếu tồn tại
if PlayerGui:FindFirstChild("LootTransferGUI") then
    PlayerGui.LootTransferGUI:Destroy()
end

-- Biến lưu trạng thái Chế độ: "Transfer" hoặc "Teleport"
local currentMode = "Transfer"

-- 1. Khởi tạo ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "LootTransferGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = PlayerGui

-- 2. Khung giao diện chính (Main Frame)
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 360, 0, 520)
mainFrame.Position = UDim2.new(0.5, -180, 0.5, -260)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.Parent = screenGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 8)
mainCorner.Parent = mainFrame

-- Tiêu đề GUI
local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(1, -70, 0, 35)
titleLabel.Position = UDim2.new(0, 10, 0, 5)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Loot Manager"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextSize = 18
titleLabel.Font = Enum.Font.SourceSansBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = mainFrame

-- Nút Làm mới (Refresh)
local refreshBtn = Instance.new("TextButton")
refreshBtn.Name = "RefreshButton"
refreshBtn.Size = UDim2.new(0, 30, 0, 30)
refreshBtn.Position = UDim2.new(1, -65, 0, 5)
refreshBtn.BackgroundColor3 = Color3.fromRGB(50, 120, 200)
refreshBtn.Text = "🔄"
refreshBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
refreshBtn.TextSize = 16
refreshBtn.Parent = mainFrame

local refreshCorner = Instance.new("UICorner")
refreshCorner.CornerRadius = UDim.new(0, 6)
refreshCorner.Parent = refreshBtn

-- Nút Tắt (Close)
local closeBtn = Instance.new("TextButton")
closeBtn.Name = "CloseButton"
closeBtn.Size = UDim2.new(0, 30, 0, 30)
closeBtn.Position = UDim2.new(1, -30, 0, 5)
closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
closeBtn.Text = "X"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.TextSize = 16
closeBtn.Font = Enum.Font.SourceSansBold
closeBtn.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeBtn

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- 3. Thanh chuyển đổi Chế độ (Mode Selector)
local modeFrame = Instance.new("Frame")
modeFrame.Name = "ModeFrame"
modeFrame.Size = UDim2.new(1, -20, 0, 32)
modeFrame.Position = UDim2.new(0, 10, 0, 42)
modeFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
modeFrame.BorderSizePixel = 0
modeFrame.Parent = mainFrame

local modeCorner = Instance.new("UICorner")
modeCorner.CornerRadius = UDim.new(0, 6)
modeCorner.Parent = modeFrame

local transferModeBtn = Instance.new("TextButton")
transferModeBtn.Name = "TransferModeBtn"
transferModeBtn.Size = UDim2.new(0.5, -2, 1, -4)
transferModeBtn.Position = UDim2.new(0, 2, 0, 2)
transferModeBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 250)
transferModeBtn.Text = "📦 Mode: Remote Transfer"
transferModeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
transferModeBtn.TextSize = 12
transferModeBtn.Font = Enum.Font.SourceSansBold
transferModeBtn.Parent = modeFrame

local btnCorner1 = Instance.new("UICorner")
btnCorner1.CornerRadius = UDim.new(0, 4)
btnCorner1.Parent = transferModeBtn

local teleportModeBtn = Instance.new("TextButton")
teleportModeBtn.Name = "TeleportModeBtn"
teleportModeBtn.Size = UDim2.new(0.5, -2, 1, -4)
teleportModeBtn.Position = UDim2.new(0.5, 0, 0, 2)
teleportModeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 52)
teleportModeBtn.Text = "⚡ Mode: Teleport + [F]"
teleportModeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
teleportModeBtn.TextSize = 12
teleportModeBtn.Font = Enum.Font.SourceSansBold
teleportModeBtn.Parent = modeFrame

local btnCorner2 = Instance.new("UICorner")
btnCorner2.CornerRadius = UDim.new(0, 4)
btnCorner2.Parent = teleportModeBtn

-- 4. Ô nhập liệu tìm kiếm (Search Box)
local searchBox = Instance.new("TextBox")
searchBox.Name = "SearchBox"
searchBox.Size = UDim2.new(1, -20, 0, 32)
searchBox.Position = UDim2.new(0, 10, 0, 80)
searchBox.BackgroundColor3 = Color3.fromRGB(45, 45, 52)
searchBox.BorderSizePixel = 0
searchBox.PlaceholderText = "Nhập tên vật phẩm (ví dụ: Metal Parts)..."
searchBox.PlaceholderColor3 = Color3.fromRGB(150, 150, 160)
searchBox.Text = ""
searchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
searchBox.TextSize = 14
searchBox.Font = Enum.Font.SourceSans
searchBox.ClearTextOnFocus = false
searchBox.Parent = mainFrame

local searchCorner = Instance.new("UICorner")
searchCorner.CornerRadius = UDim.new(0, 6)
searchCorner.Parent = searchBox

-- 5. Khung cuộn danh sách (ScrollingFrame)
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ItemList"
scrollFrame.Size = UDim2.new(1, -20, 1, -128)
scrollFrame.Position = UDim2.new(0, 10, 0, 118)
scrollFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 22)
scrollFrame.BorderSizePixel = 0
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.ScrollBarThickness = 6
scrollFrame.Parent = mainFrame

local scrollCorner = Instance.new("UICorner")
scrollCorner.CornerRadius = UDim.new(0, 6)
scrollCorner.Parent = scrollFrame

local uiList = Instance.new("UIListLayout")
uiList.Parent = scrollFrame
uiList.SortOrder = Enum.SortOrder.LayoutOrder
uiList.Padding = UDim.new(0, 5)

uiList:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    scrollFrame.CanvasSize = UDim2.new(0, 0, 0, uiList.AbsoluteContentSize.Y + 10)
end)

-- 6. Xử lý Chuyển đổi Mode
transferModeBtn.MouseButton1Click:Connect(function()
    currentMode = "Transfer"
    transferModeBtn.BackgroundColor3 = Color3.fromRGB(50, 150, 250)
    transferModeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    teleportModeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 52)
    teleportModeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
end)

teleportModeBtn.MouseButton1Click:Connect(function()
    currentMode = "Teleport"
    teleportModeBtn.BackgroundColor3 = Color3.fromRGB(220, 120, 30)
    teleportModeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    transferModeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 52)
    transferModeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
end)

-- 7. Các hàm chức năng chính
local function transferItem(itemId)
    local remote = ReplicatedStorage:FindFirstChild("CLIENT_REMOTES") and ReplicatedStorage.CLIENT_REMOTES:FindFirstChild("TransferItem")
    if remote then
        remote:FireServer(itemId, "Main", "Storage")
        print("Đã gửi Remote TransferItem cho ID:", itemId)
    else
        warn("Không tìm thấy RemoteEvent: ReplicatedStorage.CLIENT_REMOTES.TransferItem")
    end
end

local function teleportAndInteract(targetInstance)
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end

    -- Xác định tọa độ CFrame của vật phẩm
    local targetCFrame = nil
    if targetInstance:IsA("Model") then
        targetCFrame = targetInstance:GetPivot()
    elseif targetInstance:IsA("BasePart") then
        targetCFrame = targetInstance.CFrame
    else
        -- Trường hợp là Folder hoặc Object chứa part bên trong
        local part = targetInstance:FindFirstChildWhichIsA("BasePart", true)
        if part then
            targetCFrame = part.CFrame
        end
    end

    if targetCFrame then
        -- Teleport nhân vật tới sát vị trí vật phẩm
        character.HumanoidRootPart.CFrame = targetCFrame * CFrame.new(0, 1, 1.5)
        task.wait(0.1)

        -- Tự động giả lập bấm phím F
        VirtualInputManager:SendKeyPressEvent(Enum.KeyCode.F, true, game)
        task.wait(0.05)
        VirtualInputManager:SendKeyPressEvent(Enum.KeyCode.F, false, game)
        print("Đã Teleport và bấm phím F tới:", targetInstance:GetFullName())
    else
        warn("Không thể xác định vị trí của vật phẩm trong Workspace.")
    end
end

-- 8. Quét và hiển thị danh sách
local function scanLootables()
    for _, child in ipairs(scrollFrame:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end

    local lootablesFolder = workspace:FindFirstChild("Lootables")
    if not lootablesFolder then
        warn("Không tìm thấy thư mục workspace.Lootables")
        return
    end

    local filterText = string.lower(searchBox.Text)

    for _, item in ipairs(lootablesFolder:GetDescendants()) do
        local itemId = item:GetAttribute("ID")
        if itemId then
            local itemName = tostring(item:GetAttribute("ItemName") or item.Name)
            local itemType = tostring(item:GetAttribute("Type") or "Unknown")

            if filterText == "" or string.find(string.lower(itemName), filterText, 1, true) then
                local itemBtn = Instance.new("TextButton")
                itemBtn.Name = "ItemBtn_" .. itemName
                itemBtn.Size = UDim2.new(1, -10, 0, 42)
                itemBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 48)
                itemBtn.Text = string.format("  [%s] %s\n  ID: %s", itemType, itemName, tostring(itemId))
                itemBtn.TextColor3 = Color3.fromRGB(220, 220, 220)
                itemBtn.TextSize = 13
                itemBtn.Font = Enum.Font.SourceSans
                itemBtn.TextXAlignment = Enum.TextXAlignment.Left
                itemBtn.Parent = scrollFrame

                local btnCorner = Instance.new("UICorner")
                btnCorner.CornerRadius = UDim.new(0, 4)
                btnCorner.Parent = itemBtn

                -- Click xử lý dựa theo Mode đang chọn
                itemBtn.MouseButton1Click:Connect(function()
                    if currentMode == "Transfer" then
                        transferItem(itemId)
                    elseif currentMode == "Teleport" then
                        teleportAndInteract(item)
                    end
                end)
            end
        end
    end
end

-- Sự kiện tìm kiếm & Refresh
searchBox.FocusLost:Connect(scanLootables)
searchBox:GetPropertyChangedSignal("Text"):Connect(scanLootables)
refreshBtn.MouseButton1Click:Connect(scanLootables)

-- Quét dữ liệu ban đầu
scanLootables()
